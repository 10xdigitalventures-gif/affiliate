# Copy to .env.docker for local Docker infrastructure only.
# Generate both values independently, for example: openssl rand -hex 32
POSTGRES_PASSWORD=REPLACE_WITH_32_PLUS_RANDOM_CHARACTERS
REDIS_PASSWORD=REPLACE_WITH_32_PLUS_RANDOM_CHARACTERS
